export const metadata = {
  title: "Catálogo Proxy",
  description: "API para extrair metadados de produtos."
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body>{children}</body>
    </html>
  );
}
